# PPT 图片转可编辑 PPTX 提示词（GPT 6 Pro 网页版）

请使用以下开源项目/技能完成重建：

https://github.com/shizaishiwo323/rebuild-scientific-poster-ppt

我已按顺序上传整套 16:9 PPT 页面图片。请严格按文件名中的页码顺序，将每张图片分别重建为可编辑幻灯片，并最终合并成一个可编辑 `.pptx` 文件发给我。

## 重建要求

1. 保持原图片的页面顺序、16:9 尺寸、网格、留白、字体层级、浙大蓝配色、右上角 Logo、图表位置和视觉比例。
2. 标题、正文、页码、图注、标签、关键结论、流程图文字和表格文字必须重建为独立可编辑文本框；尽量使用本机可用的近似字体，禁止把整页图片直接铺满当作“可编辑 PPT”。
3. 流程图、边框、色块、箭头、编号圆点和基础示意图优先重建为 PowerPoint 原生形状，保证颜色、线宽、圆角、对齐和层级可编辑。
4. 论文结果图、照片、复杂机制图和无法可靠矢量化的科研图像，应从原页面中高质量裁切为独立图片对象；不得让裁切混入相邻文字或背景，也不得改变数据内容。
5. 使用 `浙大logo.png` 原文件作为每页右上角 Logo，不从页面截图中二次裁切 Logo。
6. 所有对象不得越界、遮挡或错位；正文保持足够字号，图片保持清晰，不拉伸变形。
7. 严格保留原始页序，并使用与图片一致的页码；若源图片缺页、重号或排序不清，先列出问题再继续，不得猜测顺序。

## 输出与验证

1. 先逐页重建，再按原顺序合并为一个 PPTX。
2. 对最终文件进行渲染回放，并逐页与源图片对照检查布局、文字、颜色、图像裁切和页面顺序。
3. 检查 PPTX 包结构和可编辑性：确认不是全页栅格图；统计可编辑文本框、原生形状和独立图片对象数量。
4. 检查乱码、字体替换、文字溢出、重叠、越界、失真、空白页和重复页，并修复后再交付。
5. 最终只交付一个按顺序合并的可编辑 PPTX；同时用简短清单报告总页数、可编辑文本框数量、原生形状数量、独立图片数量以及仍需人工复核的极少数对象。

现在请直接开始重建，不需要再次询问是否继续；完成全部页面并通过上述验证后，把最终 `.pptx` 文件发给我。

