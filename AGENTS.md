# 项目定义

本项目用于管理研究领域文献，并把文献整理成适合 GPT/Codex 项目使用的材料结构。

## 目录约定

- `原始文献布局/`：保留原始 PDF、补充材料、下载清单和主题分类。
- `gpt项目布局/`：面向 GPT 项目的整理结果，包括合并 PDF、内容说明、manifest、项目指令和渲染检查材料。
- 主题目录如 `反应输运/`、`震电效应/`、`NMR/`、`sip模拟/` 按研究方向组织。

## 工作原则

- 优先维护可追溯性：原始文献、整理后的 GPT 项目材料、manifest 或清单要能互相对应。
- 新增文献时，先放入对应主题的 `原始文献布局/`，再按需要生成或更新 `gpt项目布局/`。
- 不随意重命名已有文献文件；确需重命名时，同步更新相关清单和说明文件。
- 不批量删除文件或目录。需要删除时，只能一次删除一个明确路径的文件。
- 默认 Python 环境使用 conda；需要运行 Python 前，先检查所需库在哪个 conda 环境中存在。

## GPT 项目输出要求

- 每个主题的 GPT 项目材料应尽量包含：项目指令、PDF 内容说明、source manifest、合并或精选文献包。
- 说明文件使用中文，文件名尽量清楚表达主题和用途。
- 生成检查图、渲染结果或临时清单时，保留能证明材料可读性的最小必要文件。
